# Simple Python Calculator

A terminal-based Python calculator that follows standard operator precedence using step-by-step user input.

## Features
- Handles operator precedence (`*` and `/` before `+` and `-`)
- Interactive step-by-step input (enter numbers and operators one by one)
- No parentheses support
